package com.domain.repository;

import com.domain.entity.TypeClass;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TypeClassRepository extends JpaRepository<TypeClass, Long> {
}
